"use strict";

/* !
 ! Identifier: [
    first: Alpha, _ $
    other: above, number
  ],

 ! keyWord: [ (can be object.attribute)
    ES6: {
      // break, class,
      do, extends,
      in, return,
      typeof, while,
      case, const,
      else, for,
      instanceof, switch,
      var,  yield,
      catch,  debugger,
      export, function,
      new,  this,
      void, default,
      if, throw,
      delete, import,
      try,
    },

    future : [
       alaways: enum,
       inStrict: [
          implements, package, public, interface, protected, static, let, private
       ],
       Module : [
        await
       ]
    ]
  ]
 */
